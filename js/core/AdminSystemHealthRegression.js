// WorldProject – Regression für zentrale Admin-Systemgesundheit.
import { adminSystemHealth } from './AdminSystemHealth.js';
export function runAdminSystemHealthRegression(){const checks=[],check=(name,ok,detail=null)=>checks.push({name,success:!!ok,detail});try{const cleanWorld={currency:{base:'EUR',rates:{EUR:1,USD:1,GBP:1,JPY:1,CNY:1,KRW:1,CHF:1,PLN:1,CZK:1,SEK:1,NOK:1,DKK:1,CAD:1,AUD:1,BRL:1,INR:1,TRY:1}},suppliers:[{id:'s1',active:true,reliability:.95,leadTimeHours:24}],npcs:[{id:'n1',active:true,budget:100,marketWeight:1}],supportState:{cases:[]}},clean=adminSystemHealth({world:cleanWorld,companies:[]});check('clean score high',clean.score>=90,clean.score);check('clean no support breach',clean.support.breached===0);const badWorld={currency:{base:'EUR',rates:{EUR:1}},suppliers:[{id:'s1',active:false}],npcs:[{id:'n1',active:true,budget:-5,marketWeight:20}],supportState:{cases:[{id:'c1',priority:'urgent',status:'open',createdAt:1,updatedAt:1}]}};const bad=adminSystemHealth({world:badWorld,companies:[]});check('bad has issues',bad.issues.length>0);check('bad currency warning',bad.currency.missing.length>0);check('bad supplier critical',bad.issues.some(x=>x.code==='suppliers'));check('bad npc warning',bad.issues.some(x=>x.code==='npc_invalid'));check('bad support critical',bad.issues.some(x=>x.code==='support_sla'));}catch(error){check('health exception',false,error.message);}const failed=checks.filter(x=>!x.success),result={success:!failed.length,total:checks.length,passed:checks.length-failed.length,failed,checks};if(typeof console!=='undefined')console[result.success?'log':'error'](`WORLDPROJECT ADMIN HEALTH ${result.passed}/${result.total}`,result);return result;}
if(typeof window!=='undefined')window.runAdminSystemHealthRegression=runAdminSystemHealthRegression;
