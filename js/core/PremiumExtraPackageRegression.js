import { packageQuote, activateExtraPackage, configureExtraPackage, claimDailyExtraPackageMaterials } from './PremiumExtraPackageSystem.js';
export function runPremiumExtraPackageRegression(){const checks=[],ok=(name,v)=>checks.push({name,success:!!v});const original=packageQuote('construction_materials',7);ok('7 Tage vorhanden',original.days===7);ok('Paket vor Balancing gesperrt',original.enabled===false);configureExtraPackage('construction_materials',{enabled:true,priceByDays:{7:4.99},dailyMaterials:{concrete:10,steel:1}});const account={},company={constructionSite:{materials:{},jobs:[]}},start=Date.now(),entry=activateExtraPackage(account,{packageId:'construction_materials',days:7,now:start});ok('Aktivierung getrennt von Premiumzeit',!('premiumUntil' in account)&&entry.days===7);const credit=claimDailyExtraPackageMaterials(account,company,{packageInstanceId:entry.id,now:start});ok('Baustoffe direkt Baustelle',credit.concrete===10&&company.constructionSite.materials.concrete===10&&!company.inventory);let duplicate=false;try{claimDailyExtraPackageMaterials(account,company,{packageInstanceId:entry.id,now:start});}catch{duplicate=true;}ok('Nur eine Tagesgutschrift',duplicate);configureExtraPackage('construction_materials',{enabled:false,priceByDays:{7:null},dailyMaterials:{concrete:null,steel:null}});const failed=checks.filter(x=>!x.success);const result={success:!failed.length,total:checks.length,passed:checks.length-failed.length,failed,checks};console[result.success?'log':'error'](`WORLDPROJECT EXTRA-PREMIUM ${result.passed}/${result.total}`,result);return result;}
