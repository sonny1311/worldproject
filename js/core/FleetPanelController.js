// WorldProject – Fuhrpark-/Lieferungsansicht für bestehende UI.
const n=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const done=new Set(['received','completed','cancelled','arrived']);
const statusLabel=s=>({open:'Offen',ordered:'Bestellt',planned:'Geplant',loading:'Wird beladen',in_transit:'Unterwegs',underway:'Unterwegs',arrived:'Angekommen',received:'Eingelagert',completed:'Abgeschlossen',cancelled:'Storniert'}[s]||'In Bearbeitung');
function etaMs(d){const raw=d.eta||d.arrivalAt||d.expectedAt||null;if(!raw)return null;const ms=typeof raw==='number'?raw:new Date(raw).getTime();return Number.isFinite(ms)?ms:null;}
export function fleetPanelState(company,{now=Date.now()}={}){const vehicles=company.vehicles||company.fleet||[],deliveries=company.inboundDeliveries||company.deliveries||[];const rows=deliveries.map(d=>{const eta=etaMs(d),status=d.status||'open',overdue=eta!==null&&eta<=now&&!done.has(status);return{id:d.id,supplier:d.supplierName||d.supplierId||'Lieferant',material:d.materialName||d.item||d.materialId||'Ware',quantity:n(d.quantity),status,statusLabel:overdue?'Ankunft überfällig':statusLabel(status),eta,remainingMs:eta===null?null:Math.max(0,eta-now),overdue,vehicleId:d.vehicleId||null,usesOwnVehicle:Boolean(d.vehicleId)};});return{vehicles:vehicles.map(v=>({id:v.id||v.instanceId,label:v.name||v.label||v.type||'Fahrzeug',type:v.type,status:v.status||'available',capacityKg:n(v.capacityKg||v.payloadKg),condition:n(v.condition,100),available:!['busy','maintenance','broken'].includes(v.status)})),deliveries:rows,active:rows.filter(d=>!done.has(d.status)).length,overdue:rows.filter(d=>d.overdue).length,availableVehicles:vehicles.filter(v=>!['busy','maintenance','broken'].includes(v.status)).length};}
if(typeof window!=='undefined')window.worldFleetPanel={state:fleetPanelState};
