// WorldProject – technische Fehler in verständliche Spielerhinweise + Recovery-Aktion übersetzen.
import { humanizeInternalId } from './OperationsLocalizationRegistry.js';
export function resolvePlayerError(error,context={}){const msg=String(error?.message||error||'Unbekannter Fehler'),lower=msg.toLowerCase();let code='unknown',title='Aktion nicht möglich',action=null,detail=msg,recovery='Prüfe den aktuellen Betriebszustand und versuche die Aktion erneut.';if(/nicht genug geld|liquidität reicht|firmenkonto/.test(lower)){code='insufficient_funds';title='Nicht genug Geld';action={id:'finance',label:'Finanzen öffnen'};recovery='Kontostand, offene Zahlungen und Finanzierung prüfen oder eine kleinere Menge wählen.';}else if(/maschine.*fehlt|machine.*missing|maschine ist.*nicht|maschine.*belegt/.test(lower)){code='machine_missing';title='Benötigte Maschine fehlt oder ist nicht verfügbar';action={id:'equipment',label:'Maschinen anzeigen'};recovery='Passende Maschine kaufen, warten oder eine bereits belegte Maschine erst freigeben.';}else if(/nicht genug bestand|rohstoff|material.*fehlt|missing material/.test(lower)){code='material_missing';title='Rohstoffe fehlen';action={id:'procure',label:'Einkauf öffnen'};recovery='Fehlende Menge im Einkauf bestellen und nach Ankunft den Wareneingang bestätigen.';}else if(/personal|mitarbeiter|fachkraft|braumeister/.test(lower)){code='workforce_missing';title='Benötigte Fachkraft fehlt';action={id:'hire',label:'Personal öffnen'};recovery='Die benötigte Fachkraft einstellen und sicherstellen, dass sie aktiv beschäftigt ist.';}else if(/lagerkapazität|lager voll|nicht genug platz|capacity/.test(lower)){code='storage_full';title='Lagerkapazität reicht nicht';action={id:'warehouse',label:'Lager prüfen'};recovery='Bestand abbauen, Ware verkaufen oder Lagerkapazität erweitern, bevor weitere Ware eingelagert wird.';}else if(/lieferung.*angekommen|wareneingang/.test(lower)){code='receipt_pending';title='Wareneingang muss bestätigt werden';action={id:'fleet',label:'Lieferungen öffnen'};recovery='Die angekommene Lieferung öffnen und Wareneingang / Einlagern bestätigen.';}else if(/lieferung|transport/.test(lower)){code='transport_issue';title='Transport kann nicht ausgeführt werden';action={id:'transport',label:'Lieferungen prüfen'};recovery='Fahrzeug, Kapazität, Lieferstatus und Ankunftszeit prüfen.';}else if(/konflikt|409|conflict/.test(lower)){code='state_conflict';title='Der Spielstand wurde zwischenzeitlich geändert';action={id:'reload',label:'Aktuellen Stand neu laden'};recovery='Aktuellen Spielstand neu laden und die Aktion danach erneut ausführen.';}return{code,title,detail,recovery,action,technicalMessage:msg,entity:context.entityId?humanizeInternalId(context.entityId):null,context};}
export function errorToastVM(error,context={}){const r=resolvePlayerError(error,context);return{severity:r.code==='unknown'?'error':'warning',title:r.title,message:r.detail,recovery:r.recovery,action:r.action};}
if(typeof window!=='undefined')window.worldPlayerErrors={resolve:resolvePlayerError,toast:errorToastVM};
