// WorldProject – technische Fehler in verständliche Spielerhinweise + Recovery-Aktion übersetzen.
import { humanizeInternalId } from './OperationsLocalizationRegistry.js';
export function resolvePlayerError(error,context={}){const msg=String(error?.message||error||'Unbekannter Fehler'),lower=msg.toLowerCase();let code='unknown',title='Aktion nicht möglich',action=null,detail=msg;if(/nicht genug geld|liquidität reicht|firmenkonto/.test(lower)){code='insufficient_funds';title='Nicht genug Geld';action={id:'finance',label:'Finanzen öffnen'};}else if(/maschine.*fehlt|machine.*missing|maschine ist.*nicht/.test(lower)){code='machine_missing';title='Benötigte Maschine fehlt';action={id:'equipment',label:'Maschinen anzeigen'};}else if(/nicht genug bestand|rohstoff|material.*fehlt|missing material/.test(lower)){code='material_missing';title='Rohstoffe fehlen';action={id:'procure',label:'Einkauf öffnen'};}else if(/personal|mitarbeiter|fachkraft/.test(lower)){code='workforce_missing';title='Personal fehlt';action={id:'hire',label:'Personal öffnen'};}else if(/lagerkapazität|lager voll|capacity/.test(lower)){code='storage_full';title='Lagerkapazität reicht nicht';action={id:'warehouse',label:'Lager prüfen'};}else if(/lieferung|transport/.test(lower)){code='transport_issue';title='Transport kann nicht ausgeführt werden';action={id:'transport',label:'Lieferungen prüfen'};}else if(/konflikt|409|conflict/.test(lower)){code='state_conflict';title='Der Spielstand wurde zwischenzeitlich geändert';action={id:'reload',label:'Aktuellen Stand neu laden'};}return{code,title,detail,action,technicalMessage:msg,entity:context.entityId?humanizeInternalId(context.entityId):null,context};}
export function errorToastVM(error,context={}){const r=resolvePlayerError(error,context);return{severity:r.code==='unknown'?'error':'warning',title:r.title,message:r.detail,action:r.action};}
if(typeof window!=='undefined')window.worldPlayerErrors={resolve:resolvePlayerError,toast:errorToastVM};