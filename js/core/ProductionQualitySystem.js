// WorldProject - Produktionsqualitaet, Qualifikation und Ausschuss
export class ProductionQualitySystem{
 constructor({scrapBaseRate=.01}={}){this.scrapBaseRate=scrapBaseRate;this.runs=[];this.seq=1;}
 calculate({rawQuality=1,machineCondition=1,machineLevel=1,employeeSkill=1,processQuality=1}={}){const machineQuality=Math.min(1.2,Number(machineCondition)*(.98+Math.max(0,Number(machineLevel)-1)*.01)),quality=Math.max(.1,Math.min(1.25,Number(rawQuality)*.42+machineQuality*.23+Number(employeeSkill)*.20+Number(processQuality)*.15)),scrapRate=Math.max(0,Math.min(.5,this.scrapBaseRate+(1-Number(machineCondition))*.12+(1-Number(employeeSkill))*.10+(1-Number(processQuality))*.06));return{quality,scrapRate,machineQuality};}
 produce({companyId,product,plannedQuantity,...factors}={}){const q=this.calculate(factors),planned=Math.max(0,Number(plannedQuantity)),scrap=planned*q.scrapRate,good=planned-scrap,run={id:this.seq++,companyId,product,plannedQuantity:planned,goodQuantity:good,scrapQuantity:scrap,quality:q.quality,scrapRate:q.scrapRate,createdAt:Date.now()};this.runs.push(run);return run;}
}
export function runProductionQualityTest(){const p=new ProductionQualitySystem(),good=p.produce({companyId:1,product:"beer",plannedQuantity:100,rawQuality:1.1,machineCondition:1,machineLevel:2,employeeSkill:1.1,processQuality:1.05}),bad=p.produce({companyId:2,product:"beer",plannedQuantity:100,rawQuality:.8,machineCondition:.6,employeeSkill:.6,processQuality:.7});if(!(good.quality>bad.quality&&good.scrapQuantity<bad.scrapQuantity))throw new Error("Produktqualitaets-Test fehlgeschlagen");console.log("🧪 PRODUKTQUALITAET + AUSSCHUSS ERFOLGREICH");return true;}
