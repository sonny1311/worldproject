// WorldProject – Regression für Lieferanten-/NPC-Adminsteuerung.
import { adminControlSystem } from './AdminControlSystem.js';
import { setSupplierActive, setSupplierLeadTime, setSupplierReliability, supplierAdminHealth } from './AdminSupplierControl.js';
import { setNpcActive, setNpcBudget, setNpcMarketWeight, setNpcLiquidity, npcControlHealth } from './AdminNpcControl.js';
export function runAdminSupplierNpcRegression(){const checks=[],check=(name,ok,detail=null)=>checks.push({name,success:!!ok,detail});const actor={id:'supplier-npc-test',role:'admin',username:'Test'},world={suppliers:[{id:'s1',name:'S1',active:true,leadTimeHours:24,reliability:.95}],npcs:[{id:'n1',name:'NPC 1',active:true,budget:1000,marketWeight:1}]},auditBefore=adminControlSystem.auditLog.length,liquidityBefore=adminControlSystem.settings.npcLiquidityEnabled;try{setSupplierActive(actor,world,{supplierId:'s1',active:false,reason:'Regression'});check('supplier deactivate',world.suppliers[0].active===false);setSupplierActive(actor,world,{supplierId:'s1',active:true,reason:'Regression'});setSupplierLeadTime(actor,world,{supplierId:'s1',hours:48,reason:'Regression'});check('supplier lead',world.suppliers[0].leadTimeHours===48);setSupplierReliability(actor,world,{supplierId:'s1',reliability:.9,reason:'Regression'});check('supplier reliability',world.suppliers[0].reliability===.9);check('supplier health',supplierAdminHealth(world).total===1);setNpcActive(actor,world,{npcId:'n1',active:false,reason:'Regression'});check('npc deactivate',world.npcs[0].active===false);setNpcBudget(actor,world,{npcId:'n1',budget:2500,reason:'Regression'});check('npc budget',world.npcs[0].budget===2500);setNpcMarketWeight(actor,world,{npcId:'n1',weight:1.5,reason:'Regression'});check('npc weight',world.npcs[0].marketWeight===1.5);setNpcLiquidity(actor,{enabled:false,reason:'Regression'});check('npc liquidity',adminControlSystem.settings.npcLiquidityEnabled===false);check('npc health',npcControlHealth(world).invalid===0);let denied=false;try{setNpcBudget({id:'support',role:'support'},world,{npcId:'n1',budget:1,reason:'Regression'});}catch{denied=true;}check('npc role denied',denied);}catch(error){check('supplier npc exception',false,error.message);}finally{adminControlSystem.settings.npcLiquidityEnabled=liquidityBefore;if(adminControlSystem.auditLog.length>auditBefore)adminControlSystem.auditLog.splice(auditBefore);}const failed=checks.filter(x=>!x.success),result={success:!failed.length,total:checks.length,passed:checks.length-failed.length,failed,checks};if(typeof console!=='undefined')console[result.success?'log':'error'](`WORLDPROJECT SUPPLIER/NPC REGRESSION ${result.passed}/${result.total}`,result);return result;}
if(typeof window!=='undefined')window.runAdminSupplierNpcRegression=runAdminSupplierNpcRegression;
