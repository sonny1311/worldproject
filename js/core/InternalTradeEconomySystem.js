// WorldProject - interne Warenpreise und Handelsmargen zwischen eigenen Betrieben
export function internalTransferValue({quantity=0,unitCost=0,markupPct=0}={}){const q=Math.max(0,Number(quantity||0)),cost=Math.max(0,Number(unitCost||0)),markup=Math.max(0,Number(markupPct||0))/100;return {quantity:q,unitCost:cost,unitTransferPrice:cost*(1+markup),totalCost:q*cost,totalTransferValue:q*cost*(1+markup)};}

export function sellTradeInventory(company,{product,quantity,unitPrice}={}){company.tradeInventory??={};company.tradeSales??=[];const q=Math.max(0,Number(quantity||0)),stock=Number(company.tradeInventory[product]||0),price=Math.max(0,Number(unitPrice||0));if(!product||q<=0)throw new Error("Produkt und Menge erforderlich");if(stock<q)throw new Error("Nicht genug Handelsbestand");const avgCost=Number(company.tradeInventoryCost?.[product]||0),revenue=q*price,cost=q*avgCost,profit=revenue-cost;company.tradeInventory[product]=stock-q;company.money=Number(company.money||0)+revenue;const sale={id:`trade-sale-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,product,quantity:q,unitPrice:price,revenue,cost,profit,createdAt:Date.now()};company.tradeSales.push(sale);return sale;}

export function receiveTradeInventory(company,{product,quantity,unitTransferPrice=0}={}){company.tradeInventory??={};company.tradeInventoryCost??={};const oldQty=Number(company.tradeInventory[product]||0),oldCost=Number(company.tradeInventoryCost[product]||0),q=Math.max(0,Number(quantity||0)),p=Math.max(0,Number(unitTransferPrice||0)),newQty=oldQty+q;company.tradeInventory[product]=newQty;company.tradeInventoryCost[product]=newQty?((oldQty*oldCost)+(q*p))/newQty:0;return {product,quantity:q,stock:newQty,averageCost:company.tradeInventoryCost[product]};}

export function runInternalTradeEconomyTest(){const shop={money:1000,tradeInventory:{},tradeInventoryCost:{}};receiveTradeInventory(shop,{product:"cola",quantity:100,unitTransferPrice:.5});const sale=sellTradeInventory(shop,{product:"cola",quantity:20,unitPrice:1});if(shop.tradeInventory.cola!==80||shop.money!==1020||sale.profit!==10)throw new Error("Interner Handelstest fehlgeschlagen");console.log("✅ INTERNER HANDEL: BESTAND → EINSTANDSPREIS → VERKAUF → MARGE ERFOLGREICH",{shop,sale});return true;}
