// WorldProject - Lieferantenvergleich: Spieler entscheidet selbst
export class SupplierComparisonSystem{
 compare({suppliers=[],material,quantity,qualityGrade="standard",qualitySystem=null,quoteFn=null}={}){return suppliers.filter(s=>(s.materials||[]).includes(material)).map(s=>{const qualityOffer=qualitySystem?.supplierOffer?.(s,material,qualityGrade)||{quality:Number(s.quality||1),unitPrice:Number(s.prices?.[material]||0),grade:qualityGrade};const quote=typeof quoteFn==="function"?quoteFn(s,material,quantity):{goodsCost:qualityOffer.unitPrice*quantity,deliveryCost:Number(s.deliveryBase||0)+Number(s.distanceKm||0)*Number(s.deliveryPerKm||0),deliveryHours:Number(s.deliveryHours||0),reliability:Number(s.reliability||1)};return{supplierId:s.id,label:s.label,material,quantity,qualityGrade,quality:qualityOffer.quality,unitPrice:qualityOffer.unitPrice,goodsCost:quote.goodsCost??qualityOffer.unitPrice*quantity,deliveryCost:Number(quote.deliveryCost||0),totalCost:Number((quote.goodsCost??qualityOffer.unitPrice*quantity)+Number(quote.deliveryCost||0)),distanceKm:Number(s.distanceKm||0),deliveryHours:Number(quote.deliveryHours??s.deliveryHours||0),reliability:Number(quote.reliability??s.reliability||1)};});}
 select(options,supplierId){const o=options.find(x=>x.supplierId===supplierId);if(!o)throw new Error("Gewaehlter Lieferant ist fuer diese Bestellung nicht verfuegbar");return{...o,selectedByPlayer:true};}
}
export function runSupplierComparisonTest(){const s=new SupplierComparisonSystem(),suppliers=[{id:"a",label:"A",materials:["malt"],prices:{malt:1},quality:.9,distanceKm:100,deliveryBase:20,deliveryPerKm:.2,deliveryHours:20,reliability:.95},{id:"b",label:"B",materials:["malt"],prices:{malt:1.2},quality:1.1,distanceKm:20,deliveryBase:10,deliveryPerKm:.2,deliveryHours:5,reliability:.99}],o=s.compare({suppliers,material:"malt",quantity:100});if(o.length!==2||!s.select(o,"b").selectedByPlayer)throw new Error("Lieferantenvergleich-Test fehlgeschlagen");console.log("🧾 LIEFERANTENVERGLEICH MIT FREIER SPIELERWAHL ERFOLGREICH");return true;}
