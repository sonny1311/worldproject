// WorldProject – Lageransicht für bestehende UI.
const n=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
export function inventoryPanelState(company){const legacyRaw=company.inventory||{},finished=company.finishedGoods||{},op=company.operationalSupplyState?.warehouseStock||null,reservations=company.warehouse?.reservations||[],lots=company.warehouse?.lots||[],reservedByItem={};for(const r of reservations.filter(x=>x.status==='active'))reservedByItem[r.item]=n(reservedByItem[r.item])+n(r.quantity);const operational={},zoneByItem={};if(op)for(const[zone,stock]of Object.entries(op))for(const[id,q]of Object.entries(stock||{})){operational[id]=n(operational[id])+n(q);zoneByItem[id]=zone;}const raw=Object.keys(operational).length?operational:legacyRaw;const items=[...new Set([...Object.keys(raw),...Object.keys(finished)])].map(id=>({id,zone:zoneByItem[id]||(Object.prototype.hasOwnProperty.call(finished,id)?'finished':'raw'),raw:n(raw[id]),finished:n(finished[id]),reserved:n(reservedByItem[id]),free:Math.max(0,n(raw[id])+n(finished[id])-n(reservedByItem[id])),lots:lots.filter(x=>x.item===id&&n(x.quantity)>0).length}));const byZone={raw:[],packaging:[],finished:[],cold:[],other:[]};for(const item of items)(byZone[item.zone]||byZone.other).push(item);return{items,byZone,lots,reservations,critical:items.filter(x=>x.free<=0),totalItems:items.length,source:Object.keys(operational).length?'operationalSupplyState':'legacyInventory'};}
if(typeof window!=='undefined')window.worldInventoryPanel={state:inventoryPanelState};
